{
    "scripts": {
      "preinstall": "bash ./setup-flutter.sh",
      "dev": "export PATH=\"$PATH:$HOME/flutter/bin\" && flutter run -d web-server --web-hostname 0.0.0.0 --web-port 8080",
      "install": "export PATH=\"$PATH:$HOME/flutter/bin\" && flutter pub get"
    }
  }